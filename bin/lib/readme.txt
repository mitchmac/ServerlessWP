Libraries needed by binaries in Lambda should go in this directory.

Building binaries with build_bin.sh and the related Dockerfile in the parent directory should take care of this.

It should contain:
libcrypto.so.10
