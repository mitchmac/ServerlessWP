name: Stream Wrapper Tests
on:
  push:
    paths:
      - 'wp-alt-streamwrapper/**'
      - 'wp/wp-content/mu-plugins/wp-alt-streamwrapper**'
      - '.github/workflows/streamwrapper.yml'
  pull_request:
    paths:
      - 'wp-alt-streamwrapper/**'
      - 'wp/wp-content/mu-plugins/wp-alt-streamwrapper**'
      - '.github/workflows/streamwrapper.yml'
jobs:
  unit:
    name: unit tests
    runs-on: ubuntu-latest
    if: github.repository_owner == 'mitchmac'
    defaults:
      run:
        working-directory: ./wp-alt-streamwrapper
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.3'

      - name: Install dependencies
        run: composer install --no-interaction --prefer-dist

      - name: Run unit tests
        run: vendor/bin/phpunit --testsuite unit

  deployable-copy:
    name: deployable copy is current
    runs-on: ubuntu-latest
    if: github.repository_owner == 'mitchmac'
    defaults:
      run:
        working-directory: ./wp-alt-streamwrapper
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Compare wp/wp-content/mu-plugins against the source
        run: ./build-plugin.sh --check

  e2e:
    name: e2e tests
    runs-on: ubuntu-latest
    if: github.repository_owner == 'mitchmac'
    needs: unit
    defaults:
      run:
        working-directory: ./wp-alt-streamwrapper
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Install dependencies
        run: docker run --rm -v "$PWD:/app" -w /app composer:2 install --no-interaction --prefer-dist

      - name: Run E2E tests
        run: ./run-tests.sh e2e
